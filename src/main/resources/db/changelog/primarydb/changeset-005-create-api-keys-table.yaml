databaseChangeLog:
  - changeSet:
      id: 005
      author: barbara
      changes:
        - createTable:
            tableName: api_keys
            columns:
              - column:
                  name: api_key_id
                  type: BIGINT
                  constraints:
                    primaryKey: true
                    nullable: false
                    unique: true
              - column:
                  name: api_key
                  type: VARCHAR(50)
                  constraints:
                    nullable: false
